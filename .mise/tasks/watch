#!/usr/bin/env bash
#MISE description="Watch the latest workflow run until completion"

set -e

RUN_ID=$(gh run list --workflow=run.yml --limit 1 --json databaseId -q '.[0].databaseId')
gh run watch "$RUN_ID"
