#!/usr/bin/env bash
#MISE description="Run all checks (test, format, lint)"

set -e

cd shimmer-cli

echo "=== Running tests ==="
mix test

echo ""
echo "=== Checking formatting ==="
mix format --check-formatted

echo ""
echo "=== Running linter ==="
mix credo

echo ""
echo "✓ All checks passed"
