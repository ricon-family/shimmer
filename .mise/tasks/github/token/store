#!/usr/bin/env bash
#MISE description="Store a GitHub PAT in the secret provider"
#USAGE arg "<agent>" help="Agent name (e.g., k7r2, brownie)"
#USAGE arg "<token>" help="The GitHub PAT to store"
#USAGE example "mise run github:token:store zeke ghp_example" header="Store an agent token"

set -e

AGENT="${usage_agent:?Usage: shimmer token:store <agent> <token>}"
TOKEN="${usage_token:?Usage: shimmer token:store <agent> <token>}"

printf '%s' "$TOKEN" | secrets set "$AGENT/github-pat"

echo "Stored PAT for $AGENT"
